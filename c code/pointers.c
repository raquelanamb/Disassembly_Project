/*
    Demonstrates pointer dereferencing and memory loads.
    Shows: load instructions, register usage, pointer arithmetic,
    and how each ISA handles reading from memory.
*/

int pointer(int *p) {
    return *p + 3;
}
